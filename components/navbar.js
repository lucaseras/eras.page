// React component that defines the navigation bar
import styles from '../styles/navbar.css'

export default function Navbar() {

    return (
        <nav className={styles.nav}>
            <un className={styles.un}>
        </nav>
    )
}
